#ifndef PARSE_H
#define PARSE_H

token_t **parser(
	const garbage_collector_t *GC,
	token_t **raw_tokens
);

	PT[PI]->literal = str_copy(             \
		RT[RI]->literal, 0);                \
	PT[PI]->type = normal_string;           \
	PI += 1;

#endif /* PARSE_H */

